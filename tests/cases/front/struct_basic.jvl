struct Point {
    x: int;
    y: int;
};

func main(): int {
    var p: ptr<struct Point> = alloc(sizeof(struct Point));
    p->x = 3;
    p->y = 4;
    putint(p->x);
    putint(p->y);
    return 0;
}
