#define FEATURE_A
#define FEATURE_B

#if defined(FEATURE_A) && defined(FEATURE_B)
    #define MODE 1
#elif defined(FEATURE_A)
    #define MODE 2
#else
    #define MODE 0
#endif

func main(): int {
    putint(MODE);
    return 0;
}
